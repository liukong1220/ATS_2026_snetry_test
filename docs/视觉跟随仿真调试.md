# 视觉跟随仿真调试

## 1. 文档定位

本文只讨论当前仓库内已经可用的 loopback 视觉跟随调试链路，目标是帮助你快速回答四个问题：

1. fake vision 是否真的把 `VisionTargetMsg` 发出来了。
2. 行为树是否真的进入了视觉接管分支。
3. `SelectVisionFollowPath` 是否真的生成了跟随路径。
4. 视觉失效后，系统是否真的回退到普通决策分支。

如果你想看更完整的行为树结构，请配合阅读：

- [`./sentry_bt_decision_checklist.md`](./sentry_bt_decision_checklist.md)

如果你想确认视觉接管触发 `attack` 姿态、低血量 `defend` 姿态和受击自旋的当前规则，请同时阅读：

- [`./sentry_posture_switch_logic.md`](./sentry_posture_switch_logic.md)

如果你想看视觉消息和路径生成原理，请配合阅读：

- [`./融合.md`](./融合.md)

---

## 2. 当前有两个可用入口

### 2.1 专用视觉快捷入口

文件：

- [`../src/pb2025_sentry_bringup/launch/loopback_vision_test.launch.py`](../src/pb2025_sentry_bringup/launch/loopback_vision_test.launch.py)

特点：

1. 默认加载 [`../src/pb2025_sentry_behavior/params/sentry_behavior_vision_test.yaml`](../src/pb2025_sentry_behavior/params/sentry_behavior_vision_test.yaml)
2. 默认 `target_tree = vision_test`
3. 默认开启 `publish_vision_target`
4. 底层仍然复用 `loopback_decision_sim.launch.py`

适合场景：

- 只想快速验证视觉接管链路

### 2.2 通用 loopback 入口

文件：

- [`../src/pb2025_sentry_bringup/launch/loopback_decision_sim.launch.py`](../src/pb2025_sentry_bringup/launch/loopback_decision_sim.launch.py)

特点：

1. 默认加载 [`../src/pb2025_sentry_behavior/params/sentry_behavior_loopback.yaml`](../src/pb2025_sentry_behavior/params/sentry_behavior_loopback.yaml)
2. 默认 `target_tree = rmul_2026`
3. 默认 `publish_vision_target = False`

适合场景：

- 想验证“普通决策 + 视觉 override”在主树里的真实优先级

---

## 3. 当前视觉跟随链路到底是什么

当前 loopback 里的视觉接管链路可以直接理解成：

```text
fake_decision_sim_inputs.py
  -> 发布 /vision/target
  -> behavior server 写入黑板 sp_vision_target
  -> IsVisionTargetValid
  -> PublishGimbalAbsolute
  -> SelectVisionFollowPath
  -> SendNavThroughPoses
  -> Nav2 + MPPI 执行
```

当前最关键的输入不是旧的目标索引，而是：

- `target_position_map`
- `has_target_position_map`
- `target_position_map_frame`

这意味着当前视觉跟随已经是“地图级目标点驱动”，而不是“给定固定点编号驱动”。

---

## 4. 当前涉及的关键文件

### 4.1 行为树与插件

- [`../src/pb2025_sentry_behavior/behavior_trees/vision_test.xml`](../src/pb2025_sentry_behavior/behavior_trees/vision_test.xml)
- [`../src/pb2025_sentry_behavior/plugins/condition/is_vision_target_valid.cpp`](../src/pb2025_sentry_behavior/plugins/condition/is_vision_target_valid.cpp)
- [`../src/pb2025_sentry_behavior/plugins/action/select_vision_follow_path.cpp`](../src/pb2025_sentry_behavior/plugins/action/select_vision_follow_path.cpp)
- [`../src/pb2025_sentry_behavior/plugins/action/send_nav_through_poses.cpp`](../src/pb2025_sentry_behavior/plugins/action/send_nav_through_poses.cpp)
- [`../src/pb2025_sentry_behavior/plugins/condition/is_path_goal_reached.cpp`](../src/pb2025_sentry_behavior/plugins/condition/is_path_goal_reached.cpp)

### 4.2 假输入与启动

- [`../src/pb2025_sentry_bringup/scripts/fake_decision_sim_inputs.py`](../src/pb2025_sentry_bringup/scripts/fake_decision_sim_inputs.py)
- [`../src/pb2025_sentry_bringup/launch/loopback_vision_test.launch.py`](../src/pb2025_sentry_bringup/launch/loopback_vision_test.launch.py)
- [`../src/pb2025_sentry_bringup/launch/loopback_decision_sim.launch.py`](../src/pb2025_sentry_bringup/launch/loopback_decision_sim.launch.py)

### 4.3 参数文件

- [`../src/pb2025_sentry_behavior/params/sentry_behavior_vision_test.yaml`](../src/pb2025_sentry_behavior/params/sentry_behavior_vision_test.yaml)
- [`../src/pb2025_sentry_behavior/params/sentry_behavior_loopback.yaml`](../src/pb2025_sentry_behavior/params/sentry_behavior_loopback.yaml)

---

## 5. 推荐启动方式

### 5.1 用专用视觉入口

```bash
source /opt/ros/humble/setup.bash
colcon build --packages-select pb2025_sentry_behavior pb2025_sentry_bringup --symlink-install
source install/setup.bash
ros2 launch pb2025_sentry_bringup loopback_vision_test.launch.py \
  use_rviz:=True \
  vision_tracking:=True \
  vision_nav_hold:=True \
  vision_target_yaw:=0.30 \
  vision_target_pitch:=-0.06 \
  vision_target_position_map_x:=5.0 \
  vision_target_position_map_y:=2.0 \
  vision_target_position_map_z:=0.0 \
  vision_has_target_position_map:=True
```

这条命令的含义是：

1. 使用 `vision_test` 行为树
2. 默认开启假视觉消息发布
3. 让视觉目标满足接管条件
4. 直接给出一个地图坐标目标

### 5.2 用主树入口测试 override

```bash
source install/setup.bash
ros2 launch pb2025_sentry_bringup loopback_decision_sim.launch.py \
  use_rviz:=True \
  publish_vision_target:=True \
  vision_tracking:=True \
  vision_nav_hold:=True \
  vision_target_position_map_x:=5.0 \
  vision_target_position_map_y:=2.0 \
  vision_target_position_map_z:=0.0 \
  vision_has_target_position_map:=True
```

这条命令的含义是：

1. 仍然跑 `rmul_2026`
2. 只是在主树里把视觉 override 打开
3. 更接近后续真实实机场景

---

## 6. 运行中最常用的检查项

### 6.1 先看 fake vision 是否真的在发

```bash
ros2 topic echo --once /vision/target
```

重点检查：

1. `tracking: true`
2. `nav_hold: true`
3. `has_target_position_map: true`
4. `target_position_map_frame: map`
5. `target_position_map` 是有限值

注意：

- 当前代码并不会因为 `target_position_map = 0,0,0` 就自动判它无效
- 真正的必要条件是 `has_target_position_map=true`、frame 非空且点是有限值

### 6.2 在线切换目标地图点

```bash
ros2 param set /fake_decision_sim_inputs vision_target_position_map_x 2.5
ros2 param set /fake_decision_sim_inputs vision_target_position_map_y 4.2
ros2 param set /fake_decision_sim_inputs vision_target_position_map_z 0.0
```

预期现象：

1. 行为树日志出现新的 `Vision follow target=(...) selected_goal=(...)`
2. 如果新目标足够偏离旧目标，会重新发 `Send NavigateThroughPoses goal with 1 poses`
3. RViz 中 marker 会更新

### 6.3 在线调整视觉跟随参数

```bash
ros2 param set /pb2025_sentry_behavior_server decision.vision.attack_radius 2.5
```

当前常用参数：

- `decision.vision.attack_radius`
- `decision.vision.follow_occupied_threshold`
- `decision.vision.follow_sample_count`
- `decision.vision.follow_arc_half_angle_deg`
- `decision.vision.min_replan_interval_s`
- `decision.vision.min_goal_shift_m`

### 6.4 验证视觉失效后的回退

```bash
ros2 param set /fake_decision_sim_inputs vision_tracking false
```

或者：

```bash
ros2 param set /fake_decision_sim_inputs publish_vision_target false
```

预期现象：

1. `IsVisionTargetValid` 失败
2. `vision_test` 下会回退到 `decision_simulation`
3. `rmul_2026` 下会回退到 simulation 或 referee 分支

---

## 7. 当前 RViz 里应该看什么

当前推荐重点观察：

1. `/decision/vision_follow_markers`
2. 全局路径
3. 局部轨迹 / trajectories
4. 机器人当前位姿

当前 `SelectVisionFollowPath` 会发布的 marker 语义是：

1. 红点：视觉目标地图点
2. 青色弧线：围绕目标的候选跟随圆弧
3. 绿色点：最终选中的跟随点
4. 黄色线：当前位姿到跟随点的连线

注意：

- 当前代码中真正持续发布的是 `decision/vision_follow_markers`
- 不要把 RViz 里历史残留的 `vision/target_point_map` 显示项当成当前主观察来源

---

## 8. 当前日志应该怎么理解

### 8.1 看到 `Vision follow target=(...) selected_goal=(...)`

表示：

1. 视觉目标有效
2. 当前位姿有效
3. `SelectVisionFollowPath` 成功选出了一个跟随点

### 8.2 看到 `Send NavigateThroughPoses goal with 1 poses`

表示：

1. 当前视觉分支生成的是单点 path
2. 该 path 已经被发给 Nav2

这是正常现象，不是“少发了 waypoint”。

### 8.3 没有反复看到发送日志，不代表没工作

`SendNavThroughPoses` 现在有“同一路径不重发”的逻辑。

所以当：

1. 视觉目标没变
2. 跟随点没明显变化
3. 当前 goal 仍在执行

你可能不会每个 tick 都看到新发 goal 的日志，这恰恰说明节流与去重在工作。

### 8.4 到点判定现在怎么工作

当前 `IsPathGoalReached` 的逻辑是：

1. 如果输入端口里已经有 `goal_succeeded`，优先使用它
2. 否则再退回到几何距离判定

这意味着：

- 在 `vision_test` 和当前主树里，若 Nav2 已明确返回成功，行为树会优先相信 action 结果

---

## 9. 当前最常见的误区

### 9.1 误区一：视觉目标在发，行为树就一定会接管

不是。

还必须同时满足：

1. `tracking = true`
2. 若要求接管，则 `nav_hold = true`
3. 时间戳没超时
4. `target_yaw / target_pitch` 有效

### 9.2 误区二：目标点一变，就应该立刻重发路径

不是。

当前还有两层节流：

1. `min_replan_interval_s`
2. `min_goal_shift_m`

如果变化太小，系统会复用旧路径，这是当前设计的一部分。

### 9.3 误区三：普通巡逻回退后一定会出现 `2 poses`

不是。

当前巡逻逻辑已经把 `patrol_preview_points` 实际钳制成单目标点发送，因此普通巡逻回退后，日志里依然很可能看到：

- `Send NavigateThroughPoses goal with 1 poses`

### 9.4 误区四：只要点是零值就一定无效

不是。

当前真正决定可不可用的条件是：

1. `has_target_position_map`
2. `target_position_map_frame`
3. 点是否有限
4. TF 是否可变换
5. 当前位姿是否可用

---

## 10. 推荐排查顺序

建议按这条顺序排查：

1. `ros2 topic echo --once /vision/target`
2. `ros2 action info /navigate_through_poses`
3. 看 `SelectVisionFollowPath` 日志
4. 看 `Send NavigateThroughPoses goal with 1 poses` 日志
5. 看 RViz 里的 `decision/vision_follow_markers`
6. 最后再去调视觉参数和 MPPI 参数

这样能把问题切开：

1. 消息没发出来，是输入层问题
2. 消息发出来但不接管，是条件层问题
3. 接管了但不出 path，是选点/TF/costmap 问题
4. path 出来了但车走得差，是 Nav2 / MPPI 问题

---

## 11. 一句话总结

当前视觉仿真调试最重要的三个抓手是：

1. `vision/target` 是否满足接管条件
2. `SelectVisionFollowPath` 是否成功生成单点跟随 path
3. `decision/vision_follow_markers` 和 `NavigateThroughPoses` 是否与日志一致

只要这三层对上了，后续再调的就是参数，而不是链路有没有打通。
