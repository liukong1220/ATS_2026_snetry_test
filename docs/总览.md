# 项目总览

## 简介

ATS 哨兵当前正式 ROS 2 导航为自研 ROGMap、RC-ESDF、MINCO、Goal Manager 和全向 MPC 链。

## 模块

根仓拥有 bringup/配置，导航仓拥有算法，MuJoCo 仓拥有物理仿真，行为仓通过 ATS action 下发目标。

## 依赖

ROS 2 Humble 与各包 `package.xml` 依赖；`参考/` 不进入活动构建。

## 构建

```bash
MAKEFLAGS=-j1 colcon build --base-paths src --symlink-install --parallel-workers 1
```

## 启动

`bringup.launch.py` 和 `real_robot_navigation.launch.py` 固定自研导航，MuJoCo 使用
`rmuc_2026_mujoco.launch.py`。

## 接口

ATS action 是 `/ats_navigate_to_pose`；当前目标速度链是
`/cmd_vel/autonomy_raw ->（实机可选 fake/chassis 变换）-> cmd_vel_arbiter -> /cmd_vel/selected`。
`/cmd_vel` 保留为手动输入，不再作为自主最终输出。

## 配置

根 `node_params.yaml` 是正式节点参数总 YAML；launch 不提供第二份算法参数源。

## 架构

```text
Point-LIO -> ROGMap/adapter -> RC-ESDF -> JPS/MINCO -> Goal Manager -> SE2 MPC -> chassis
```

## 验证与限制

当前已有组件测试和部分 Gazebo/MuJoCo 证据，但最终 revision 的 P2 nominal/red-box/fault matrix、
P3 Nav2-free、P4 连续 swept 与实机导航仍未完成。仿真不以 CAN、电机、轮速、接触或底盘反馈
作为通过条件。活动执行入口为：

- [ATS 导航剩余优化总 TODO](项目优化文档/ATS导航剩余优化总TODO.md)
- [ATS 自研导航当前状态](nav2_to_3desdf_minco_mpc_optimization_direction.md)
- [下一阶段新对话提示词](项目优化文档/下一阶段提示词_ATS导航仿真闭环与准入.md)
- [LTV-QP 后端准入记录](ats_swerve_mpc_ltv_qp_backend_admission.md)
