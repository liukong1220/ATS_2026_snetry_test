# 哨兵导航与决策移植说明

## 1. 当前结论

当前工程已经完成以下迁移结果：

- `rmul_2026.xml` 是新的主决策树
- 决策执行方式已经切换为 `NavigateThroughPoses`
- 默认轻量仿真不再依赖裁判系统时间、血量、RFID
- loopback 默认走 `simulation` 输入源
- 同时保留了可选的“手动裁判仿真输入”模式，便于测试比赛状态、血量、时间分支

这意味着你现在的项目已经不是 `goal_pose` 方案，而是：

- 决策层：`pb2025_sentry_behavior`
- 导航执行层：`Nav2 NavigateThroughPoses`
- 轻量仿真层：`nav2_loopback_sim`

## 2. 目录与入口

- 新主树：`src/pb2025_sentry_behavior/behavior_trees/rmul_2026.xml`
- 旧树保留：`src/pb2025_sentry_behavior/behavior_trees/rmul_2025.xml`
- 实机/通用参数：`src/pb2025_sentry_behavior/params/sentry_behavior.yaml`
- loopback 默认参数：`src/pb2025_sentry_behavior/params/sentry_behavior_loopback.yaml`
- 行为树服务端：`src/pb2025_sentry_behavior/src/pb2025_sentry_behavior_server.cpp`
- loopback 启动入口：`src/pb2025_sentry_bringup/launch/loopback_decision_sim.launch.py`
- loopback 假输入节点：`src/pb2025_sentry_bringup/scripts/fake_decision_sim_inputs.py`

### 2.1 地图与 PCD 资源入口

现在默认资源入口已经统一到：

- `src/pb2025_sentry_bringup/map`
- `src/pb2025_sentry_bringup/pcd`

这意味着：

- loopback 仿真默认从 `pb2025_sentry_bringup/map` 读取地图
- 实车导航默认从 `pb2025_sentry_bringup/map` 和 `pb2025_sentry_bringup/pcd` 读取资源
- 不再默认依赖 `pb2025_nav_bringup` 里的地图 / 点云资源作为入口

## 3. 现在的决策结构

### 3.1 顶层输入源

`rmul_2026` 现在支持两种输入源：

- `decision.input_source: simulation`
- `decision.input_source: referee`

行为树会先判断输入源，再选择对应分支：

- `simulation` 分支只看 `decision/sim_mode`
- `referee` 分支继续看比赛状态、剩余时间、当前血量

### 3.2 simulation 分支

支持四种仿真模式：

- `patrol`
- `anchor`
- `retreat`
- `safe`

对应关系：

- `patrol`：多点巡逻
- `anchor`：固定锚点驻守
- `retreat`：退防点
- `safe`：安全点

### 3.3 referee 分支

优先级从高到低：

1. 危险血量 `critical` -> 安全点
2. 低血量 `low` -> 退防点
3. 关键时间 `critical` 且血量正常 -> 关键时间固定点
4. 正常血量 -> 巡逻

## 4. 现在为什么更平滑

这次移植里，决策输出已经不是反复发布单个 `/goal_pose`，而是直接调用 `NavigateThroughPoses`：

- 巡逻路径可以一次性交给 Nav2 连续执行
- 分支不变时，不会每个 tick 重复取消并重发同一路径
- 分支切换时，会主动取消旧路径并派发新路径

同时增加了参数：

- `decision.decision_config.patrol_preview_points`

这个参数的作用是：

- 决定一次送给 `NavigateThroughPoses` 的巡逻预览点数量
- 数值越大，巡逻点足够多时越连续
- 不需要重新编译，只要改 YAML

注意：

- 如果你的 `patrol_indices` 只有 2 个点，那么 `patrol_preview_points` 调再大，提升也有限
- 真正想更丝滑，应该同时增加巡逻点数量和路径密度

运行中临时调这个参数也可以：

```bash
ros2 param set /pb2025_sentry_behavior_server decision.decision_config.patrol_preview_points 3
```

如果你是改 YAML，则重新启动 launch 即可，不需要重新编译。

### 4.1 现在为什么不会和行为树 `halt()` 冲突

这次方案里，`NavigateThroughPoses` 能稳定工作，还有一个很重要的原因：

- 我们不是直接用 `BehaviorTree.ROS2` 的 `RosActionNode`
- 而是自己实现了一个自定义节点 `SendNavThroughPoses`

对应实现位置：

- `src/pb2025_sentry_behavior/include/pb2025_sentry_behavior/plugins/action/send_nav_through_poses.hpp`
- `src/pb2025_sentry_behavior/plugins/action/send_nav_through_poses.cpp`

这样做的核心目的，是绕开 `RosActionNode` 在 `cancel during halt()` 这条链路上的已知问题。

你可以把当前方案理解成：

- 行为树负责“决定什么时候应该发一条路径”
- `SendNavThroughPoses` 负责“把这条路径异步送给 Nav2，并自己维护 goal 状态”
- 行为树本身不去长期持有一个 `RUNNING` 的 action 节点等待它结束

当前规避冲突的设计有 4 个关键点。

#### 4.1.1 `SendNavThroughPoses` 是 `SyncActionNode`

`SendNavThroughPosesAction` 继承的是：

- `BT::SyncActionNode`

而不是：

- `BT::RosActionNode`
- `BT::StatefulActionNode`

这意味着它在一次 `tick()` 里就会返回，不会长时间停留在 `RUNNING` 状态等待 Nav2 action 完成。

直接结果是：

- 上层行为树通常不需要对它执行 `halt()`
- 自然也就不会落入“halt 时顺带 cancel action”那条容易冲突的路径

#### 4.1.2 真正的 Nav2 action 由节点内部异步管理

在 `tick()` 里，这个节点会自己创建并使用：

- `rclcpp_action::Client<NavigateThroughPoses>`

然后调用：

- `async_send_goal(...)`

也就是说：

- BT 节点同步返回
- Nav2 action 在节点内部异步继续跑
- feedback 和 result 通过回调更新内部状态

这相当于把“BT 生命周期”和“ROS action 生命周期”解耦了。

#### 4.1.3 同一路径不重发，新路径才手动 cancel

当前实现不会每个 tick 都重发目标。

它先比较当前输入路径和正在执行的 `active_path_` 是否等价：

- 如果是同一路径，而且目标还在执行，就直接返回成功，不重发
- 如果是同一路径，而且已经成功完成，就直接设置 `goal_succeeded`
- 只有路径真的变了，才会先 `cancelCurrentGoal()` 再发新 goal

这点非常关键，因为它避免了两类常见问题：

- 行为树高频 tick 导致的重复发目标
- 每次重 tick 都 cancel 一次，最终把 Nav2 和 BT 状态搅乱

所以现在的取消逻辑不是由 `halt()` 触发的，而是由“路径切换”主动触发的。

#### 4.1.4 用 `goal_request_id_` 屏蔽旧回调串线

当前实现里还有一个小但很重要的保护：

- 每次发新 goal 都会递增 `goal_request_id_`
- feedback / goal response / result 回调都会先检查 request id

这样做的效果是：

- 旧 goal 即使晚到反馈，也不会污染新 goal 的状态
- 取消旧目标后，不容易发生“旧回调把当前状态写乱”的问题

这对 Reactive 行为树场景尤其重要，因为分支切换时目标替换会比较频繁。

### 4.2 从行为树视角看它的执行方式

当前 `rmul_2026.xml` 里，对每种决策路径基本都用了同一个模式：

1. 先生成 `decision_path`
2. 先用 `IsPathGoalReached` 判断当前路径是不是已经完成
3. 只有未完成时，才执行 `SendNavThroughPoses`

也就是说行为树不是“卡在导航 action 上等它返回”，而是每次 tick 都做一次：

- 这条路径还要不要继续
- 如果要继续，当前 goal 是否已经在跑
- 如果已经在跑且路径没变，就什么都不做

因此整个模式更像：

- “路径命令派发器”

而不是：

- “BT 内部阻塞等待的 action 节点”

这也是它和 `halt()` 不容易冲突的根本原因。

### 4.3 一句话总结

当前项目避免 `NavigateThroughPoses` 与行为树 `halt()` 冲突的方法，不是去修 `halt()`，而是从架构上绕开它：

- 用自定义 `SyncActionNode`
- 在节点内部异步维护 Nav2 action
- 同路径不重发
- 仅在路径切换时主动 cancel
- 用 request id 防止旧回调串线

所以你现在这套方案的本质是：

- `NavigateThroughPoses` 仍然是真执行接口
- 但它不再受 `BehaviorTree.ROS2` 默认 action/halt 生命周期的直接约束

## 5. 可直接改 YAML 的核心参数

下面这些都已经统一成参数，不需要为了改它们重新编译。

### 5.1 输入源与仿真模式

- `decision.input_source`
- `decision.simulation.default_mode`
- `decision.simulation.mode_topic`

### 5.2 决策目标点与角色

- `decision.goal_points.x`
- `decision.goal_points.y`
- `decision.goal_points.z`
- `decision.point_roles.supply_safe_point_index`
- `decision.point_roles.patrol_indices`
- `decision.point_roles.low_hp_candidate_indices`
- `decision.point_roles.critical_time_target_index`
- `decision.point_roles.anchor_target_index`

### 5.3 比赛状态判定

- `decision.referee.start_gate.expected_game_progress`
- `decision.referee.start_gate.min_remain_time`
- `decision.referee.start_gate.max_remain_time`
- `decision.time_thresholds.*`
- `decision.hp_thresholds.*`

### 5.4 NavigateThroughPoses 相关

- `decision.decision_config.nav2_action_server`
- `decision.decision_config.goal_position_tolerance`
- `decision.decision_config.patrol_preview_points`
- `decision.decision_config.action_server_wait_timeout_s`

### 5.5 控制话题与默认旋转

- `decision.topics.spin`
- `decision.topics.cmd_vel`
- `decision.motion.default_spin_speed`

## 6. 轻量 loopback 默认运行方式

默认启动：

```bash
source install/setup.bash
ros2 launch pb2025_sentry_bringup loopback_decision_sim.launch.py
```

如果你手动指定，也推荐写成：

```bash
ros2 launch pb2025_sentry_bringup loopback_decision_sim.launch.py use_rviz:=true
```

默认行为：

- 使用 `sentry_behavior_loopback.yaml`
- `decision.input_source=simulation`
- `fake_decision_sim_inputs.py` 发布 `decision/sim_mode`
- 行为树执行 `rmul_2026`

运行中切换模式：

```bash
ros2 param set /fake_decision_sim_inputs decision_mode patrol
ros2 param set /fake_decision_sim_inputs decision_mode anchor
ros2 param set /fake_decision_sim_inputs decision_mode retreat
ros2 param set /fake_decision_sim_inputs decision_mode safe
```

如果想完全自己手动发仿真模式 topic：

```bash
ros2 launch pb2025_sentry_bringup loopback_decision_sim.launch.py \
  use_rviz:=False \
  publish_decision_mode:=False
```

然后手动发布：

```bash
ros2 topic pub /decision/sim_mode std_msgs/msg/String "{data: anchor}" -1
```

## 7. 手动裁判仿真输入模式

如果你想在仿真里测试“比赛状态 + 时间 + 血量”分支：

```bash
source install/setup.bash
ros2 launch pb2025_sentry_bringup loopback_decision_sim.launch.py \
  use_rviz:=true \
  publish_referee_inputs:=True \
  publish_decision_mode:=False \
  behavior_params_file:=/home/ats/ats_sentry_ws/install/pb2025_sentry_behavior/share/pb2025_sentry_behavior/params/sentry_behavior.yaml
```

这个模式下：

- 行为树走 `referee` 输入源
- `fake_decision_sim_inputs.py` 周期发布裁判话题
- 你可以实时改参数测试分支

实时修改血量：

```bash
ros2 param set /fake_decision_sim_inputs current_hp 100
```

实时修改比赛剩余时间：

```bash
ros2 param set /fake_decision_sim_inputs stage_remain_time 30
```

实时修改比赛阶段：

```bash
ros2 param set /fake_decision_sim_inputs game_progress 4
```

如果你想完全自己手动发布裁判话题，也可以：

```bash
ros2 launch pb2025_sentry_bringup loopback_decision_sim.launch.py \
  use_rviz:=False \
  publish_referee_inputs:=False \
  publish_decision_mode:=False \
  behavior_params_file:=/home/ats/ats_sentry_ws/install/pb2025_sentry_behavior/share/pb2025_sentry_behavior/params/sentry_behavior.yaml
```

然后自行向这些话题发布：

- `/referee/game_status`
- `/referee/robot_status`
- `/referee/rfid_status`

## 8. 实车部署

### 8.1 先回答你的两个问题

1. 旧的 `robot_decision_node` 不应该再作为主决策入口继续启动。
2. 实车不要启动 `loopback_navigation.launch.py`，它是轻量仿真链路的一部分，不是实车导航入口。

当前工程里，推荐的实车总入口是：

- `src/pb2025_sentry_bringup/launch/bringup.launch.py`

这个入口已经会同时拉起：

- `standard_robot_pp_ros2` 串口与底层节点
- 实车导航入口 `rm_navigation_reality_launch.py`
- `pb2025_sentry_behavior` 行为树服务端与客户端

并且在这个入口里，传给 `standard_robot_pp_ros2.launch.py` 的参数已经是：

```python
"launch_robot_decision": "False"
```

也就是说：

- 如果你用 `bringup.launch.py` 启动，旧决策节点本来就不应该启动
- 不需要再去用 `loopback_navigation.launch.py` 顶替实车导航

### 8.2 现在代码中的实际状态

当前 `standard_robot_pp_ros2.launch.py` 里，旧 `robot_decision_node` 这一段本身就是注释状态。

所以目前结论是：

- 旧决策节点已经退出主链
- 实车主决策入口已经是 `pb2025_sentry_behavior`

### 8.3 实车推荐启动方式

最推荐直接使用一条总启动命令：

```bash
source install/setup.bash
ros2 launch pb2025_sentry_bringup bringup.launch.py \
  world:=rmul \
  use_rviz:=false
```

如果你要在实车端本机也打开 RViz，可以改成：

```bash
source install/setup.bash
ros2 launch pb2025_sentry_bringup bringup.launch.py \
  world:=rmul \
  use_rviz:=true
```

其中：

- `world:=rmul` 对应你实际使用的地图名
- `map` 和 `prior_pcd_file` 会按这个名字自动拼接
- 行为树参数默认使用 `sentry_behavior.yaml`

### 8.4 如果你想分开启动

虽然不推荐，但如果你确实要拆开启动，正确关系应该是：

1. 启动底层与串口：

```bash
source install/setup.bash
ros2 launch standard_robot_pp_ros2 standard_robot_pp_ros2.launch.py \
  launch_robot_decision:=False
```

2. 启动实车导航：

```bash
source install/setup.bash
ros2 launch pb2025_nav_bringup rm_navigation_reality_launch.py
```

3. 启动行为树：

```bash
source install/setup.bash
ros2 launch pb2025_sentry_behavior pb2025_sentry_behavior_launch.py \
  params_file:=/home/ats/ats_sentry_ws/install/pb2025_sentry_behavior/share/pb2025_sentry_behavior/params/sentry_behavior.yaml
```

但更建议你还是直接用：

```bash
ros2 launch pb2025_sentry_bringup bringup.launch.py ...
```

因为它已经把三条链路接好了。

### 8.5 实车和仿真的入口区别

你后面可以按下面这个原则记：

- 仿真总入口：`loopback_decision_sim.launch.py`
- 仿真导航入口：`loopback_navigation.launch.py`
- 实车总入口：`bringup.launch.py`
- 实车导航入口：`rm_navigation_reality_launch.py`

所以：

- `loopback_navigation.launch.py` 不能当实车行为树入口
- 实车行为树应该跟着 `bringup.launch.py` 一起启动

### 8.6 实车启动前检查项

建议你在实车部署前至少确认这几项：

1. 串口设备名、波特率和底层参数已经在 `node_params.yaml` 或串口配置中对应正确
2. `world` 对应的地图 yaml 和 pcd 文件真实存在
3. `sentry_behavior.yaml` 中的目标点、巡逻点、血量阈值都是你当前车的版本
4. 实车使用 `decision.input_source: referee`
5. 裁判系统话题、里程计、TF、激光数据都能正常发布

### 8.7 一句结论

如果你现在要上实车，正确理解是：

- 不要启动 `loopback_navigation.launch.py`
- 不要再让旧 `robot_decision_node` 参与主链
- 直接用 `bringup.launch.py` 作为实车总入口

## 9. 已验证内容

本次重构后已经完成的有效检查包括：

- `colcon build --packages-select pb2025_sentry_behavior pb2025_sentry_bringup --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release --parallel-workers 10` 编译通过
- 默认 loopback 模式下，已确认：
  - `rmul_2026` 被正常请求执行
  - `input_source=simulation`
  - Nav2 收到 `NavigateThroughPoses`
  - 仿真模式从 `patrol` 切到 `anchor` / `retreat` 可以生效
- 手动裁判仿真模式下，已确认：
  - `input_source=referee`
  - 修改 `current_hp` 后，目标可切换到安全点
  - 修改 `stage_remain_time` 后，目标可切换到关键时间点

## 10. 后续调参建议

如果你后面追求更高的平滑性，建议按下面顺序调：

1. 先把 `NavigateThroughPoses` 继续保留为主执行方式
2. 增加 `decision.point_roles.patrol_indices` 的点数
3. 把 `decision.decision_config.patrol_preview_points` 调到 3 或 4
4. 再去调 Nav2 的 planner、controller、smoother 参数

其中第 2 和第 3 步最直接。

如果只有 2 个巡逻点，就算已经切到 `NavigateThroughPoses`，丝滑程度也还是有限。

## 11. 视觉融合后的新增拓扑

在这次移植基础上，现在又额外接入了一条“视觉 -> 行为树 -> 导航/云台”的融合链，但它**没有改掉你当前的主决策模式**：

1. 主决策仍然是 `pb2025_sentry_behavior`
2. 默认主树仍然可以保持 `rmul_2026.xml`
3. 视觉融合先放在独立测试树 `vision_test.xml`

### 11.1 新增的接口包

这次没有把视觉消息塞进 `pb_rm_interfaces`，而是单独新建了：

- [/home/ats/ats_sentry_ws/src/pb2025_sentry_nav/sp_msgs](/home/ats/ats_sentry_ws/src/pb2025_sentry_nav/sp_msgs)

当前视觉融合核心消息是：

- [/home/ats/ats_sentry_ws/src/pb2025_sentry_nav/sp_msgs/msg/VisionTargetMsg.msg](/home/ats/ats_sentry_ws/src/pb2025_sentry_nav/sp_msgs/msg/VisionTargetMsg.msg)

这样做的结果是：

1. `sp_vision25` 可以直接依赖 `sp_msgs`
2. 行为树也可以直接订阅同一份消息
3. 调试时 `ros2 topic echo /vision/target` 会更清楚

### 11.2 视觉接入后的通信关系

当前拓扑可以简化成：

```text
sp_vision25
  -> 发布 /vision/target (sp_msgs/msg/VisionTargetMsg)
  -> pb2025_sentry_behavior_server 订阅后写入黑板 {@sp_vision_target}
  -> vision_test.xml 里的视觉分支读取黑板
  -> 输出 cmd_gimbal + NavigateThroughPoses
```

其中新增测试树在：

- [/home/ats/ats_sentry_ws/src/pb2025_sentry_behavior/behavior_trees/vision_test.xml](/home/ats/ats_sentry_ws/src/pb2025_sentry_behavior/behavior_trees/vision_test.xml)

新增节点包括：

1. `IsVisionTargetValid`
2. `SelectVisionTargetPath`

### 11.3 视觉建议点索引现在已经能真正参与决策

`VisionTargetMsg.suggested_goal_index` 现在不再只是占位字段了。

视觉侧已经在：

- [/home/ats/ats_sentry_ws/sp_vision25/tasks/omniperception/decider.cpp](/home/ats/ats_sentry_ws/sp_vision25/tasks/omniperception/decider.cpp)
- [/home/ats/ats_sentry_ws/sp_vision25/configs/sentry.yaml](/home/ats/ats_sentry_ws/sp_vision25/configs/sentry.yaml)

实现了第一代可配置规则：

1. 近距离目标优先压到 `close_range_goal_index`
2. 正前目标推荐 `front_goal_index`
3. 左右目标按 yaw 正负推荐 `left_goal_index / right_goal_index`

行为树侧再通过 `SelectVisionTargetPath` 把这个索引转换成 `nav_msgs/Path`。

### 11.4 现在的测试入口

如果你想先验证视觉融合，不动主树，可以直接启动：

```bash
source install/setup.bash
ros2 launch pb2025_sentry_bringup loopback_vision_test.launch.py
```

这个入口会：

1. 复用原有 `loopback_decision_sim.launch.py`
2. 自动切到 `sentry_behavior_vision_test.yaml`
3. 默认开启假视觉目标 `/vision/target`

也就是说，移植后的主链没有被破坏，只是额外多出了一条专门给视觉融合使用的可测试支路。
